#include <stdio.h>
#include <assert.h>
#include "gc.h"
int main(void) {
  int i;
  GC INIT();
  for(i=0;i<1000000; ++i){
    int **p = (int**) GC_MALLOC(sizeof(int));
    assert(*p==0);
    *p=(int*) GC_REALLOC(q, 2 * sizeof(int));
    if(i% 100000==0)
    printf("heap size= %d\n", GC get heap size());
  }
  return 0;
}